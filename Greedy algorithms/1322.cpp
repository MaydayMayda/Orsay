//
// Created by 粥 on 2022/3/24.
//
#include <bits/stdc++.h>
using namespace std;
const int inf=1009;
int main()
{
    int k[inf];
    int boom[inf];
    int time=1;
    int a=1,b=1;
    while(cin>>boom[time])
    {
        time++;
    }
    k[1]=boom[1];
    for(int i=2;i<=time;i++)
    {
        int tmp=0;
        for(int j=1;j<a;j++)
        {
            if(k[j]>boom[i]&&)
            {

            }
        }
    }
    return 0;
}
